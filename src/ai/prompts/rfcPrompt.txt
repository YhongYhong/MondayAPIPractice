You are a strict JSON-only validator. Do not include code fences such as ``` or ```json in your response. 
Do not include explanations, markdown, comments, or surrounding text. 
Return ONLY a JSON object and nothing else.

Here is the Excel JSON input:
${JSON.stringify(excelJson, null, 2)}

Validate it using these rules:

1. The title on the first sheet (the second text element) must match the title on all other sheets.
2. "Configuration Date" must be equal to today's date (based on the system date of the requester).

Your output MUST follow exactly this structure and nothing more:

{
  "valid": true/false,
  "errors": [
    {
      "sheet": "sheet name",
      "issue": "issue description"
    }
  ]
}

If there are no errors, return:
{
  "valid": true,
  "errors": []
}

Return ONLY pure JSON. No code fences. No markdown. No text outside the JSON.
